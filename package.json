{
  "name": "ezair-workspace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev:vite": "vite",
    "build:vite": "vite build",
    "preview:vite": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run test && npm run build:vite",
    "security:scan": "node scripts/security-scan.cjs",
    "smoke": "node scripts/smoke-check.cjs"
  },
  "devDependencies": {
    "@vitejs/plugin-legacy": "^5.4.3",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
